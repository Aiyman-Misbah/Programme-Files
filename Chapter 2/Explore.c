#include <stdio.h>

int main()
{
    int i = 10;
    int j = i;
    int a=1,b=2,c=3,d=4;
    //shift+alt+down arrow=made 2 copies of printf line.
    //Pressing alt can summon multiple cursor in the document.
    // ctrl+forward slash=comment.
    printf("The value of j is %d and i is %d\n", j, i);
    printf("The value of a is %d and b is %d\n", a, b);
    printf("The value of c is %d and d is %d\n", c, d);
    return 0;
}