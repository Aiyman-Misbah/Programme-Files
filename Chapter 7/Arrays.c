#include<stdio.h>

int main(){
    int marks[90];//Reserve space to store 90 interger
    marks[0]=95;
    marks[1]=80;
    //We can go all the way till marks[89]
    printf("The marks 0 and marks 1 is %d and %d",marks[0],marks[1]);
    return 0;
}