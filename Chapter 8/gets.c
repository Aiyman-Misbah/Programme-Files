#include <stdio.h>

int main()
{
    // char st[30];
    // gets(st); // Reads a string from standard input (stdin) until the user presses Enter.The entered string is stored in st!
    // printf("%s",st);
    // puts(st); // Prints a string to standard output (stdout).Prints the string & places the cursor on the next line.
    printf("Hello");
    
    
    
    return 0;
}